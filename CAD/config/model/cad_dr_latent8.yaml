include: config/model/cad_dr.yaml

# Ablation: n_compress_tokens = 8 (very aggressive compression)
# n_transit remains at 60 (same context window)
# Available space after compression: 60 - 8 - 1 = 51 tokens

n_compress_tokens: 8
